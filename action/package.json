{
  "name": "typediff-action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "bundle": "ncc build dist/main.js -o dist-action --no-source-map-register",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^6.0.0",
    "minimatch": "^10.0.0",
    "typediff": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
